import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'plantilla1_uagro',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
